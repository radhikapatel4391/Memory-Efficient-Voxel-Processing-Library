import voxel as vc
from scipy.sparse import random
import time as t
import numpy as np
from scipy import ndimage
import os
import psutil

filename = 'gyroidUniform.npy'   
process = psutil.Process(os.getpid())
print(process.memory_info())
#.............test2........		
ini = np.load(filename, mmap_mode="r")
print(process.memory_info())